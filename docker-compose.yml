services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: aisolutioncrm
      POSTGRES_PASSWORD: aisolutioncrm_dev_pw
      POSTGRES_DB: aisolutioncrm_dev
    ports:
      - "5433:5432"
    volumes:
      - crm_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aisolutioncrm -d aisolutioncrm_dev"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql://aisolutioncrm:aisolutioncrm_dev_pw@postgres:5432/aisolutioncrm_dev
      JWT_SECRET: dev-secret-change-me
      INTERNAL_BOT_SECRET: dev-internal-bot-secret
      BOT_PUSH_URL: http://bot:8080
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    volumes:
      - ./backend:/app

  bot:
    build: ./bot
    environment:
      BOT_TOKEN: ${BOT_TOKEN:-000000000:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA}
      INTERNAL_SECRET: dev-internal-bot-secret
      CRM_API_URL: http://backend:8000
      LISTEN_HOST: 0.0.0.0
      LISTEN_PORT: 8080
    ports:
      - "8080:8080"
    depends_on:
      - backend
    volumes:
      - ./bot:/app

  frontend:
    build: ./frontend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next

volumes:
  crm_postgres_data:
